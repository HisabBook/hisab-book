// src/pages/settings/components/LanguageSettings.jsx

// Your component is likely defined like this:
export const LanguageSettings = () => {
  // ... component code
};

// OR like this:
const LanguageSettings = () => {
  // ... component code
};
// export { LanguageSettings }; // another way of named export

// --- FIX ---
// Add this line at the bottom of the file
export default LanguageSettings;
